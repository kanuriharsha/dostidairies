## Copy this file to `.env` and fill with real values for local development.
# Vite exposes env vars prefixed with VITE_ to the client.

# Backend API base URL the frontend will call (example values):
# For local development:
VITE_BACKEND_URL=http://localhost:4001
# For deployed backend (Render) use your service URL, e.g.:
# VITE_BACKEND_URL=https://dostidairies-backend.onrender.com
